# Example environment variables
DATABASE_URL=

# Individual connection parameters (alternative to DATABASE_URL)
PGHOST=
PGPORT=
PGDATABASE=
PGUSER=
PGPASSWORD=

# Directory paths for Railway single-volume deployment
# For Railway: Mount volume at /volume and use subdirectories
GIT_REPOS_PATH=./.git-repos
UPLOADS_PATH=./uploads

ALLOW_SIGNUP=
SIGNUP_LIMIT=
